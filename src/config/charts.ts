export const chartOptions = {
  responsive: true,
  plugins: {
    legend: {
      position: "top" as const,
      fontColor: "white",
    },
    title: {
      display: false,
      fontColor: "white",
    },
  },
};
